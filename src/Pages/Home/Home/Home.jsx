import HelmetTitle from '../../../components/HelmetTitle';

const Home = () => {
    return (
        <section>
            <HelmetTitle title="Home" />
            home
        </section>
    );
};

export default Home;
